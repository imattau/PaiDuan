{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV", "ADMIN_PUBKEYS"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "dev": {
      "cache": false
    }
  }
}
